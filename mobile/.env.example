# Local override for the backend URL the app talks to (optional — for local
# dev without this, it falls back to app.json's extra.apiBaseUrl, which
# points at http://localhost:8000/api). Production/preview builds set this
# per EAS build profile instead — see eas.json.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api
